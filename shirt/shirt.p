import Pillow
import sys

def main():
        if len(sys.argv) < 3:
                sys.exit('Too few args')
        if len(sys.argv) > 3:
                sys.exit('Too many args')
        if sys.argv[1] 
